[project]
name = "madmans-notes"
readme = "README.md"
version = "0.1.0"
requires-python = ">=3.14"

dependencies = [
    "ipykernel>=7.3.0",
    "pytest>=9.1.1",
    "ruff>=0.16.3",
    "ty>=0.0.72",
]

[tool.uv]
package = false
